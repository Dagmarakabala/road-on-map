Po porbraniu repozytorium należy wpisać w konsoli:
### `npm install`

A następnie po pobraniu wszystkiego:

### `npm start`

Nastąpi uruchomienie aplikacji, domyślnie znajduje się pod adresem http://localhost:3000